import { ThemeOptions } from "@material-ui/core/styles";

const theme: ThemeOptions = {
  palette: {
    mode: "dark",
    secondary: {
      main: "#ff69b4",
    },
  },
  typography: {
    htmlFontSize: 16,
  },
};

export default theme;
